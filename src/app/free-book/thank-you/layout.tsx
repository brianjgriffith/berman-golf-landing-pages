import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Thank You! | The Berman Method",
};

export default function ThankYouLayout({ children }: { children: React.ReactNode }) {
  return children;
}
